import $ from 'jquery';
import 'script-loader!jquery-datetimepicker';

let main = {

    load() {

        $.noConflict();
        jQuery( document ).ready(function( $ ) {

        });

    }
};

export default main;